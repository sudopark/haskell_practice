
module Lib
    ( someFunc,
      Geo.sphereVolume,
      Geo.sphereArea,
      Geo.cubeVolume,
      Geo.cubeArea
    ) where

import Part6.Geometry as Geo

someFunc :: IO ()
someFunc = putStrLn "someFunc"
